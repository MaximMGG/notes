#include "../header/filework.h"




char **get_note_from_file(char *path, unsigned int *size) {
    FILE *f = fopen(path, "r");

    if (f == NULL) {
        f = fopen(path, "w");
        return NULL;
    }
    

    return NULL;
}
